---
status: approved
feature: harness-flow-cleanup
---

# Research Questions: harness-flow-cleanup

## Codebase

1. What does `focus` write today, and is it already equivalent to a roadmap?
2. How deep is `projects` in practice, and which parts make it feel too heavy
   for daily steering?
3. What does `morning-sync` currently read and write?
4. How does `idea` currently compose concept, brief, spec, plan, and promotion?
5. Which skill files currently exist as first-class local skills?

## Docs

1. Which Sushant workflow surfaces map to our current harness surfaces?
2. Which Sushant surfaces should be folded into existing skills instead of
   added as new top-level skills?

## Patterns

1. Which files are lightweight control planes versus durable execution memory?
2. Where should completed daily rows drain?
3. Which skills are core despite low explicit invocation count because they were
   recently introduced or trigger implicitly?

## External

No external web research needed. Source material is local:

- current `~/.dot-agent` skills and state
- current Codex session logs
- cloned `WardenJakx/dot-claude` workflow files

## Cross-Ref

1. How should the Sushant `ROADMAP.md` / `morning-sync` / `daily-review` loop be
   adapted to our shared Codex + Claude state model?
2. Which skills should be kept, folded, hidden, or cut when direct Codex usage is
   combined with strategic role?
